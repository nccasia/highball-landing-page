'use client'

import { useEffect } from 'react'
import { useAnimationState } from '@/hooks/useAnimationState'

export default function HomePage() {
  // const { setCurrentPage, isLoadingComplete } = useAnimationState()

  // useEffect(() => {
  //   if (isLoadingComplete) {
  //     setCurrentPage('home')
  //   }
  // }, [isLoadingComplete, setCurrentPage])

  return (
    <div>
    </div>
  )
}